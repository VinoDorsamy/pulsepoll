services:
  - type: web
    name: pulse-poll-cloud
    runtime: python
    plan: free
    buildCommand: echo "Pulse Poll uses Python standard library only"
    startCommand: python app.py
    healthCheckPath: /health
